export enum Role {
    GHOST = 'GHOST',
    STUDENT = 'STUDENT',
    TEACHER = 'TEACHER',
    ADMIN = 'ADMIN',
}

export enum ExamStatus {
    ONQUEUE = 'ONQUEUE',
    REJECTED = 'REJECTED',
    ACCEPTED = 'ACCEPTED' 
}